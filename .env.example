# Source/load these values into the process environment through your shell or secret manager.
# The benchmark deliberately does not read .env files itself.
DIGITALOCEAN_API_TOKEN=replace-me
OPENROUTER_API_KEY=replace-me
AZURE_INFERENCE_KEY=replace-me
